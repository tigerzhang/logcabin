package LogCabin.Server.RocksdbSnapshot;

/**
 * Snapshot format for key/value
 */

message KeyValue {
    required string key = 1;
    required string value = 2;
}

message KeyValues {
    repeated KeyValue key_value = 1;
}
